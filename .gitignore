__pycache__/
*.pyc
*.session
*.session-journal
logs/
xray
*.zip
.env
configs/failed.txt
xray.exe

# صف اهدای کاربران — محتوایش داده‌ی کاربر است و در مخزن عمومی بایگانی نمی‌شود.
configs/donations.json
configs/donations.json.tmp
# salt هش اهداکننده. با لو رفتنش هش‌های صف قابل brute-force می‌شوند و
# «شناسه‌ی تلگرام ذخیره نمی‌شود» بی‌معنی می‌شود.
configs/.donate_salt
# وضعیت ماندگار ربات (روشن/خاموش) — مخصوص همان ماشین است.
configs/bot_state.json
configs/bot_state.json.tmp
# حافظه‌ی چرخش انتشار. مخصوص همان ماشینی است که ربات را اجرا می‌کند؛ اگر
# commit شود، هر اجرای workflow آن را با وضعیتِ رانر عوض می‌کند و کانفیگی که
# ربات تازه پست کرده «ندیده» به نظر می‌آید — یعنی برگشتِ همان تکرار.
configs/publish_state.json
configs/publish_state.json.tmp
# فقط فایلِ نیمه‌کاره. خودِ configs/layer7_log.txt عمداً commit می‌شود:
# حافظه‌ی «کدام کانفیگ‌ها از تونل آزموده شده‌اند» را همان workflow می‌نویسد و
# همان می‌خواند، و بدونش هر اجرا از صفر شروع می‌کند.
configs/layer7_log.txt.tmp
