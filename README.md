# 🛡️ VPN Config Collector

جمع‌آوری خودکار کانفیگ‌های VLESS از منابع عمومی و عبور دادن‌شان از یک
pipeline هفت‌لایه. تفاوت اصلی با پروژه‌های مشابه: علاوه بر «سالم بودن سرور»،
**قابل استفاده بودن از داخل ایران** هم تست می‌شود.

## چرا لایه‌ی ایران؟

همه‌ی تست‌های محلی (TCP، TLS، xray) روی رانر گیت‌هاب در آمریکا اجرا می‌شوند.
اندازه‌گیری روی ۳۰ endpoint از خروجی تأییدشده‌ی همین پروژه:

| نقطه‌ی تست | زنده |
|-----------|------|
| نودهای آلمان / آمریکا / هلند | ۳۰ از ۳۰ |
| نودهای ایران (تهران، شیراز) | ۱ از ۳۰ |

یعنی pipeline «سرور سالم» را درست تشخیص می‌داد و «قابل استفاده برای مخاطب»
را نه — همان چیزی که در تست دستی هم دیده شد: از ۲۴۸ کانفیگ خروجی، ۸۸ تا TCP
فعال داشتند و در عمل ۴۴ تا کار می‌کردند. لایه ۳ و لایه ۴ برای بستن همین شکاف
اضافه شدند.

## لایه‌ها

| لایه | کار | نکته |
|------|-----|------|
| ۱ فرمت | پارس VLESS، رد کردن UUID و پورت خراب | `security=none` فقط با ترنسپورت CDN‌پسند قبول می‌شود |
| ۲ حذف تکراری | کلید = uuid + host + port + پارامترها | نام کانفیگ در کلید نیست |
| ۳ TCP | اتصال واقعی، چند تلاش، روی endpoint یکتا | فیلتر سخت: یک تلاش ناموفق = رد |
| ۴ ایران | check-host.net از ۷ نود ایرانی (تهران، اصفهان، شیراز) | حکم لازم است: بی‌جواب ماندن API کانفیگ را حذف نمی‌کند |
| ۵ TLS | handshake کامل با SNI درست | Reality هم پوشش داده می‌شود |
| ۶ Geo | کشور از IP، رد کردن کشورهای بلاک | برچسب کشور روی نام کانفیگ |
| ۷ HTTP | ترافیک واقعی از داخل xray، چند دور | سخت‌ترین لایه، فقط با باینری xray؛ نوبتش می‌چرخد (پایین‌تر) |

دو تصمیم که بیشترین اثر را داشتند:

- **تست روی endpoint یکتا، نه روی هر کانفیگ.** در یک پول واقعی ۳۵۱۱ کانفیگ
  فقط ۲۴۶۶ endpoint یکتا داشت؛ سه کانفیگ روی یک `host:port` یک تست می‌گیرند.
- **استثنای CDN بدون TLS.** `security=none` روی IP کلودفلر با ترنسپورت
  ws/httpupgrade/xhttp/grpc اندازه‌گیری‌شده ۲۳ از ۲۴ از ایران جواب می‌دهد،
  در حالی که IP خام VPS حدود ۱ از ۳۰. این دسته عمداً قبول می‌شود.

## درسی که یک اجرای خراب داد

اجرای ۲۰:۱۱ UTC با همزمانی ۱۲ و سقف ۶۰۰ endpoint، ۵۸۱ پاسخ `HTTP 429` از
check-host گرفت و فقط ۱۳ حکم واقعی. نتیجه: ۲۲۴۳ کانفیگ لایه ۳ به ۶ رسید و
خروجی نهایی صفر شد. دو غلط ساختاری داشت، هر دو اصلاح شده:

| غلط | اصلاح |
|-----|-------|
| ۴۲۹ مثل «از ایران بسته است» شمرده می‌شد | endpoint بی‌حکم حذف نمی‌شود؛ بدون برچسب `IR` عبور می‌کند |
| نرخ درخواست ثابت و کورکورانه بود | دروازه‌ی نرخ مشترک: هر ۴۲۹ فاصله را دو برابر می‌کند، بعد از ۳۰ خطا لایه می‌ایستد |

قاعده‌ی کلی که از این اجرا بیرون آمد: **«تست نشد» با «رد شد» یکی نیست.**
سقف سهمیه، توقف زودهنگام، IPv6 و خطای سایت همه «بی‌حکم»اند — کانفیگ در پول
می‌ماند ولی برچسب تأیید ایران نمی‌گیرد. فقط جوابِ صریحِ «وصل نشد» حذف می‌کند.

## چرخشِ نوبت لایه ۷

شکایت واقعی: «حس می‌کنم ربات دیگر دنبال کانفیگ نیست و همان‌های قبلی را
می‌فرستد.» درست بود، ولی مقصر جمع‌آوری نبود. ورودیِ لایه ۷ فقط با «تأییدشده‌ی
ایران، بعد سریع‌ترین TLS» مرتب می‌شد و بودجه‌ی زمانی دُمِ صف را می‌بُرید: در
اجرای ۱۷:۴۶ تعداد ۵۱۰۳ کانفیگِ پاس‌کرده‌ی لایه ۶ هرگز به تونل نرسیدند. کشف کار
می‌کرد؛ فقط هر نیم‌ساعت همان سریع‌ترین‌ها آزموده می‌شدند.

اصلاح: `configs/layer7_log.txt` — دفتری با یک هش، شماره‌ی اجرا و نتیجه برای هر
کانفیگ. همان workflow می‌نویسدش و commit می‌کند، پس بین اجراهای گیت‌هاب می‌ماند
بدون هیچ سرویس یا volume. سه قاعده:

- هر اجرا سهمِ **تضمینی** به «هرگز آزموده‌نشده‌ها» می‌دهد (`HTTP_FRESH_SHARE`).
- صف درهم‌بافته است نه پشت‌سرهم: بودجه هرجا قطع شود همان نسبت رعایت شده. (اگر
  تازه‌ها آخرِ صف بودند و بودجه در ۶۰٪ تمام می‌شد، سهمشان صفر بود.)
- کانفیگی که آخرین حکمش «رد» بوده نه در صف می‌آید (تا `HTTP_RETRY_AFTER_RUNS`
  اجرا) و نه در پول ذخیره؛ **«تست نشد» ≠ «رد شد» دو طرف تیغ دارد.**

اولِ صف `HTTP_PROVEN_CORE` کانفیگِ تأییدشده‌ی اجرای قبل می‌نشیند تا اگر بودجه
زود تمام شد، کانال بی‌کانفیگِ واقعاً آزموده نماند. دو عددِ تازه در `stats.json`
می‌گویند چرخش کار می‌کند: `fresh_tested` (چند تا برای اولین بار آزموده شدند) و
`new_passed` (چند تای تازه تأیید شدند). همان دو عدد در `/quality` هم می‌آیند.

## آمار آخرین اجرا

<!-- STATS_START -->
| فیلد | مقدار |
|------|-------|
| آخرین آپدیت | 2026-09-04 10:50:31 UTC |
| مدت اجرا | 3225.1s |
| جمع‌آوری | 20362 |
| ✅ معتبر | **628** |
| 🇮🇷 تأییدشده از ایران | **17** |
| لایه ۱ فرمت | 17259 |
| لایه ۲ حذف تکراری | 11065 |
| لایه ۳ TCP | 8914 |
| لایه ۴ دسترسی از ایران | 8902 (105 تأییدشده) |
| لایه ۵ TLS | 6905 |
| لایه ۶ Geo | 6889 |
| لایه ۷ HTTP (3 دور) | 628 |
<!-- STATS_END -->

## خروجی‌ها

| فایل | محتوا |
|------|-------|
| `configs/valid.txt` | هرچه از هفت لایه رد شد، مرتب بر اساس تأخیر |
| `configs/iran.txt` | زیرمجموعه‌ای که از نودهای ایرانی هم جواب داد |
| `configs/top10.txt` | ۱۰ کانفیگ برتر؛ تأییدشده‌های ایران اول |
| `configs/sub_base64.txt` | نسخه‌ی Base64 برای کلاینت‌های قدیمی‌تر |
| `configs/iran_base64.txt` | همان، برای `iran.txt` |
| `configs/countries/XX.txt` | تفکیک کشوری |
| `configs/index.json` | قرارداد ماشین‌خوان: مسیر فایل‌ها + شمارش‌ها |
| `configs/health.json` | اینکه *چرا* یک اجرا خالی یا کم بود |
| `configs/stats.json` | آمار کامل هر لایه |
| `configs/layer7_log.txt` | دفترِ نوبت لایه ۷: هش کانفیگ + شماره‌ی اجرا + نتیجه (بی لینک) |
| `configs/all.txt` | خام، قبل از فیلتر |

قاعده‌ی ثابت این لایه: **هیچ فایلی با محتوای خالی نوشته نمی‌شود.** یک اجرای
ناموفق نباید لینک subscription کاربران را خالی کند؛ خروجی قبلی سر جایش
می‌ماند و دلیلش در `health.json` ثبت می‌شود.

## برچسب روی نام کانفیگ

```
#MyNode|NL|84ms|IR180
```

کشور، تأخیر، و تأخیر اندازه‌گیری‌شده از ایران. نبودن `IR` یعنی لایه ۴ آن را
تأیید نکرده. این اطلاعات روی خودِ لینک است تا ربات و کانال به فایل دوم
وابسته نباشند.

## استفاده

subscription (همه‌ی کانفیگ‌های پاس‌شده):

```
https://raw.githubusercontent.com/mmbcfgklmnm/vpn-config-collector/main/configs/valid.txt
```

فقط تأییدشده‌های ایران:

```
https://raw.githubusercontent.com/mmbcfgklmnm/vpn-config-collector/main/configs/iran.txt
```

## دو پروسه‌ی مستقل

| پروسه | کجا | کار |
|-------|-----|-----|
| collector | GitHub Actions، هر ۳۰ دقیقه | ساختن پول کانفیگ و commit در `configs/` |
| bot | Railway، همیشه روشن | پاسخ به کاربر + انتشار هر ۵ دقیقه در کانال |

انتشار در کانال عمداً فقط کار ربات است. اگر هر دو پست کنند، دو حافظه‌ی چرخش
مستقل دارند و کانفیگ تکراری داخل بازه‌ی cooldown پست می‌شود. اگر ربات
همیشه‌روشن نداری `PUBLISH_AFTER_COLLECT=1` را ست کن تا هر اجرای collector هم
یک دسته منتشر کند.

هر دوره‌ی انتشار = ۱۰ پیام جدا (هر کانفیگ با مشخصاتش) + یک پیام یازدهم با
لینک subscription. `PUBLISH_COOLDOWN` جلوی تکرار همان کانفیگ‌ها را می‌گیرد تا
کل پول به‌تدریج دیده شود.

## اجرای محلی

```bash
pip install -r requirements.txt
SKIP_TELEGRAM=true SKIP_XRAY=true python -m src.main
```

برای لایه ۷ باینری xray لازم است؛ مسیرش با `XRAY_BINARY_PATH` تنظیم می‌شود.
با `SKIP_CHECKHOST=true` لایه ۴ رد می‌شود (بدون شبکه‌ی خارجی).

فهرست نودهای ایرانی check-host را می‌توان از خود API گرفت (اسم نودها در
مستند نیست و ir6 وجود ندارد):

```bash
python tools/checkhost_nodes.py --env
```

## تنظیمات مهم

| متغیر | پیش‌فرض | کار |
|-------|---------|-----|
| `TCP_ATTEMPTS` | 2 | چند اتصال موفق برای قبولی در لایه ۳ |
| `CHECKHOST_MIN_NODES` | 2 | چند نود ایرانی از ۷ باید جواب دهند |
| `CHECKHOST_TARGET_ALIVE` | 150 | به این تعداد زنده رسید، بس است |
| `CHECKHOST_MAX_ENDPOINTS` | 300 | سقف مصرف سهمیه‌ی API در هر اجرا |
| `CHECKHOST_CONCURRENCY` | 4 | چند تست همزمان روی API |
| `CHECKHOST_MIN_GAP_SEC` | 0.8 | فاصله‌ی پایه بین درخواست‌ها (با ۴۲۹ خودکار زیاد می‌شود) |
| `CHECKHOST_MAX_429` | 30 | بعد از این تعداد ۴۲۹، لایه ۴ می‌ایستد |
| `HTTP_TEST_ROUNDS` | 3 | چند دور تست HTTP در لایه ۷ |
| `MAX_HTTP_TEST` | 4000 | سقف تعدادِ ورودی لایه ۷ |
| `HTTP_TEST_BUDGET_SEC` | 2400 | بودجه‌ی زمانی دور اولِ لایه ۷ (ترمز واقعی، نه سقف تعداد) |
| `HTTP_FRESH_SHARE` | 0.5 | چه کسری از صف لایه ۷ برای «هرگز آزموده‌نشده»ها است |
| `HTTP_PROVEN_CORE` | 30 | چند تأییدشده‌ی اجرای قبل اولِ صف بنشیند |
| `HTTP_RETRY_AFTER_RUNS` | 6 | ردشده‌ی لایه ۷ بعد از چند اجرا فرصت دوباره بگیرد |
| `HTTP_LOG_KEEP_RUNS` | 48 | حکمِ قدیمی‌تر از این تعداد اجرا فراموش می‌شود |
| `HTTP_LOG_MAX` | 20000 | سقف رکوردهای `layer7_log.txt` |
| `PUBLISH_COUNT` | 10 | چند کانفیگ در هر دوره‌ی انتشار |
| `PUBLISH_INTERVAL_MIN` | 5 | فاصله‌ی دوره‌ها (دقیقه) |
| `PUBLISH_COOLDOWN` | 6 | چند دوره یک کانفیگ تکرار نشود |
| `PUBLISH_AFTER_COLLECT` | false | collector هم پست کند؟ |

## تست

```bash
python -m pytest -q
```

۴۶۲ تست، هیچ‌کدام به شبکه وصل نمی‌شوند.


